spring:
  application:
    name: configserver

  cloud:
    config:
      server:
        git:
          uri: https://github.com/shreyasharma003/salon_config_repo
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}
          clone-on-start: true

server:
  port: 8888